import data from './data/harrypotter/data.js';
// Mostar saga de los libros
const libros = data.books

libros.forEach(title =>(title.title));

function mostrar (){   
    const libros= data.title;
    return   libros}
//import data from './data/harrypotter/data.js';


//console.log( data.title);
